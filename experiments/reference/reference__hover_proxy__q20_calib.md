# Hover-proxy — порог Q20 (гл. 2)

Подмножество GT с **e_bbox ≤ Q20** на val (низкая энергия FD в bbox). Калибровка и определение — **гл. 2**, `ard100_chapter2_analytics.md`.

| | |
|---|---|
| Weights | `runs/train/ard100_mixed640_zeroP_vidDrop03_from_vidbest_gpu0/weights/best.pt` |
| τ (e_bbox zero stream2) | 3.8035714626312256 |
| JSON | `experiments/reference/reference__hover_proxy__q20_calib.json` |

Используется в **Eval B** (гл. 3, блок I).
